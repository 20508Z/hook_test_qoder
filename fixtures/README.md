# Synthetic Qoder fixtures

All fixture bodies are synthetic canaries. `demo/*.json` covers only the Qoder mutating tool
start/result path needed for code attribution.

Fixtures must never be replaced with copied IDE logs, real prompts, code, command output, spool
events, DPAPI blobs, or user configuration backups.
